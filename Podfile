# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FlameBoss' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Gloss', '~> 1.2'
  pod 'SwiftChart'
  pod 'RealmSwift'
  pod 'Fabric'
  pod 'Crashlytics'
  pod 'SwiftWebSocket'
  pod 'RichEditorView'

  target 'FlameBossTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FlameBossUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
